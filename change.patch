diff --git a/tests/test_cli.py b/tests/test_cli.py
index e9efafeb5796a1a2b91262929e252ea23f3761db..32497d7365518f0ccf45f86cf414be86e9fa8b83 100644
--- a/tests/test_cli.py
+++ b/tests/test_cli.py
@@ -1,21 +1,22 @@
 import os
 import subprocess
 import sys
 from pathlib import Path


 def test_cli_runs(tmp_path: Path):
     log_dir = tmp_path / "logs"
     out_dir = tmp_path / "out"
+    root = Path(__file__).resolve().parents[1]
     cmd = [
         sys.executable,
-        str(Path('src') / 'cli.py'),
-        '--model', 'models/best.pt',
-        '--data', 'test_data',
+        str(root / 'src' / 'cli.py'),
+        '--model', str(root / 'models' / 'best.pt'),
+        '--data', str(root / 'test_data'),
         '--output', str(out_dir),
         '--no-save',
-        '--log-dir', str(log_dir)
+        '--log-dir', str(log_dir),
     ]
     result = subprocess.run(cmd, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, text=True)
     assert result.returncode == 0
-    assert "Precision:" in result.stdout
+    assert "Precision:" in result.stdout





